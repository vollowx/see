# `Select`

- Mixes [`InternalsAttached`](./20-InternalsAttached.md)
- Mixes [`FormAssociated`](./20-FormAssociated.md)

## Properties

name             | description
---              | ---
`required`       | Reflected boolean; `false` by default
`error`          | Boolean; `false` by default
`quick`          | Boolean; `false` by default
`offset`         | Number; `0` by default
`align`          | Reflected string
`alignStrategy`  | Reflected string; `align-strategy` as an attribute
`open`           | Boolean; `false` by default
`displayText`    | String; `display-text` as an attribute
`value`          | String
`selectedIndex`  | Number; `selected-index` as an attribute
